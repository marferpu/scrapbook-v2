<% @posts.each do |post| %>
          <div class="col pt-3">
            <div class="card-index card h-100">
              <div class="card-header card border-info mb-3">
                <p class="text-uppercase fs-6"><strong style="color:#6c648b"><%= post.title %></strong></p>
              </div>
              <div class="card-body flex-column">
                <div class="mb-4 ">
                  <small class="text-muted mt-4"> <%= time_ago_in_words(post.created_at) %> ago</small>
                </div>
                <div class="d-flex position-absolute bottom-1 end-0"><%= link_to "Ver más", post, class:"btn btn-card" %></div>
              </div>
            </div>
          </div>
        <% end %>
