<% @posts.each do |post| %>

          <div class="col pt-3">
            <div class="card-index card h-100">
              <div class="card-header card border-info">
                <p class="text-uppercase fs-6"><strong><%= post.title %></strong></p>

              </div>
              <div class="card-body d-flex flex-column">
                <div class="">
                  <small class="text-muted"> <%= time_ago_in_words(post.created_at) %> ago</small>
                </div>
                <div class="d-flex justify-content-end"><%= link_to "Ver más", post, class:"btn btn-card" %></div>
              </div>
            </div>
          </div>
        <% end %>
