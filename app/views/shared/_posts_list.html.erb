<% @posts.each do |post| %>
          <div class="col pt-3">
            <div class="card-index card h-100">
              <h5 class="text-uppercase"><%= post.title %></h3>
              <small class="text-muted">Hace <%= time_ago_in_words(post.created_at) %></small>
              <%= link_to "Detalle", post, class: "btn btn-card" %>
            </div>
          </div>
        <% end %>
